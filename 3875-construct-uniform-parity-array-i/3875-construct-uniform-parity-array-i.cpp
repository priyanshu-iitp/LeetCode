class Solution {
public:
    bool uniformArray(vector<int>& nums1) {

       //if there in 1 odd element present it could make all element odd 
       //and if no odd present then array is already cointain even element
        return true;
        
    }
};